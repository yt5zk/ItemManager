<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.itemmanager.ItemMapper">
    <insert id="create">
        INSERT INTO item
        VALUES (NULL, #{item.name}, ${item.name});
    </insert>
    <update id="update">

    </update>
    <select id="read" resultType="Item">
        SELECT name
        FROM item
        WHERE id = ${id}
    </select>
    <delete id="delete">

    </delete>
</mapper>